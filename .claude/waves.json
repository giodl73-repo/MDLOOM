{
  "default": "mdloom",
  "projects": {
    "mdloom": {
      "wavesPath": "context/waves",
      "description": "mdloom architecture, quality, and document-contract execution waves",
      "projectType": "rust-cli-toolchain",
      "stack": {
        "backend": "Rust",
        "testing": "cargo test, cargo test --test integration_tests",
        "validation": "mdloom check, mdloom draft/fix smoke tests, git diff --check"
      }
    }
  }
}
