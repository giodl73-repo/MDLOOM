# MDPORT adoption closeout

MDLOOM now emits `mdport.v1` through `mdloom compile --target mdport`.
Markdown compilation, directive resolution, diagnostics, and artifact manifests
remain MDLOOM-owned; MDPORT is only the portable transfer representation.
